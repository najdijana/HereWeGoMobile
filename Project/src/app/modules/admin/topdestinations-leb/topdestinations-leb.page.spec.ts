import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TopdestinationsLebPage } from './topdestinations-leb.page';

describe('TopdestinationsLebPage', () => {
  let component: TopdestinationsLebPage;
  let fixture: ComponentFixture<TopdestinationsLebPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TopdestinationsLebPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
